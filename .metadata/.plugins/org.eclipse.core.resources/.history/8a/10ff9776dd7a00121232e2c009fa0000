package jp.ktsystem.ambitious.designpattern.decorator;

public class Decorator {

	public int exec(int i) {

		// We can some processes here.

		// Original process.
		OriginalClass c = new OriginalClass();
		c.exec(i);

		// We can some processes here.

		return 0;
	}
}
